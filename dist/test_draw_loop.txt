{
  phase = 0;
}

[
  DRAWRECT(0, 0, 480, 360, "red");
  FOR i = 0 TO 10
    radians = ((phase + i * 36) / 180) * 3.1415;
    y = 360/2 + 80 * SIN(radians);
    DRAWCIRCLE(i * 48 + 24, y, 20, "yellow");
  FEND;
  phase = phase + 8;
]